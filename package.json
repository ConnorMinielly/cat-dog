{
  "name": "cat-dog",
  "version": "1.0.0",
  "description": "An API that will replace all occurrences of the value \"dog\" in a json structure with the value \"cat\".",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "node ./esbuild.config.js",
    "test": "node --import tsx --test tests/**/*.test.ts",
    "coverage": "node --import tsx --experimental-test-coverage --test-reporter=lcov --test-reporter-destination=./lcov.info --test tests/**/*.test.ts"
  },
  "author": "Connor Minielly",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.19",
    "@types/supertest": "^6.0.2",
    "esbuild": "^0.20.0",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.2.5",
    "supertest": "^6.3.4",
    "tsx": "^4.7.1",
    "typescript": "^5.3.3",
    "typescript-eslint": "^7.0.1"
  },
  "engines": {
    "node": "^21.0.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
